import {loadState,resetState,caughtRecruitCount} from '../systems/save.js';import {uiBox,setVirtualHandler} from '../systems/ui.js';import {unlockAudio,playMusic,setMuted} from '../systems/audio.js';
const Phaser = window.Phaser;
export class TitleScene extends Phaser.Scene{
  constructor(){super('TitleScene');}
    create(){this.state=loadState();setMuted(this.state.audioMuted);playMusic('title');this.sel=this.state.party?.length?1:0;this.opts=['NEW GAME','CONTINUE','RESET'];this.cameras.main.setBackgroundColor('#1d2634');this.add.image(0,0,'title_bg').setOrigin(0).setDisplaySize(320,224);this.add.text(160,20,'BADGER GRAPPLE RED',{fontFamily:'monospace',fontSize:17,color:'#f8f0d8',fontStyle:'bold',stroke:'#111',strokeThickness:4}).setOrigin(.5);this.add.text(160,44,'CAMPAIGN v21.76 MECHANICS',{fontFamily:'monospace',fontSize:9,color:'#ffe28a',fontStyle:'bold',stroke:'#111',strokeThickness:2}).setOrigin(.5);uiBox(this,56,136,208,68);this.menuText=this.add.text(74,148,'',{fontFamily:'monospace',fontSize:10,color:'#111',fontStyle:'bold',lineSpacing:4});this.stat=this.add.text(182,150,'',{fontFamily:'monospace',fontSize:9,color:'#444',lineSpacing:3});this.input.keyboard.on('keydown-UP',()=>this.move(-1));this.input.keyboard.on('keydown-DOWN',()=>this.move(1));this.input.keyboard.on('keydown-ENTER',()=>this.choose());this.input.keyboard.on('keydown-SPACE',()=>this.choose());setVirtualHandler(this);this.render();}
  handleVirtualButton(k){unlockAudio();if(k==='up')this.move(-1);if(k==='down')this.move(1);if(k==='a')this.choose();if(k==='b'){resetState();this.state=loadState();this.render();}}
  move(d){this.sel=Phaser.Math.Wrap(this.sel+d,0,this.opts.length);this.render();}
  render(){this.menuText.setText(this.opts.map((o,i)=>`${i===this.sel?'▶':' '} ${o}`).join('\n'));const s=this.state.stats||{};this.stat.setText(`Roster ${caughtRecruitCount(this.state)}\nWins ${s.wins||0}\nScout ${s.scouts||0}${this.state.tournament?.champion?'\nCHAMPION':''}`);}
  choose(){if(this.sel===0)this.scene.start('IntroScene');if(this.sel===1&&this.state.party?.length)this.scene.start('OverworldScene');if(this.sel===2){resetState();this.state=loadState();this.render();}}
}
