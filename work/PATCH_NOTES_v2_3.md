# Badger Grapple Red Engine v2.3

## Fixed
- Battle no longer instantly counts a single B tap as a loss.
- B now opens a forfeit prompt; B again confirms, A cancels.
- Battle controls now use the prioritized mobile input router.
- Battle command UI is moved higher and inside the visible command box.
- Deployed JavaScript now loads from `src_v2_3/` to avoid mobile cache issues.

## Verify
The note under the game should say:
`Engine v2.3 battle input fix. B no longer instantly forfeits.`
